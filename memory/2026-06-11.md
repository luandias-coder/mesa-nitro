# Daily 2026-06-11 — Cory · Trading 📈

## Live da Sala Nitro (analista field veio "Meramato" — provável ruído do parser; live real é Caio)
**Carregada: SIM** — 4 sinais no Supabase (live criada 18:23 BRT). Cron de captura 18:05 (launchd) FALHOU; live entrou por outro caminho (cloud tardio ou run manual do Luan neste Mac).

### Sinais do dia (4)
- **LREN3** — saída PUT LRENS146 (07:43) + entrada CALL LRENG157 @0,75 (08:29) — virada baixa→alta confirmada.
- **BEEF3** — entrada CALL BEEFG386 @0,21 (11:36) — bidirecional, mantém PUT + compra CALL (strangle).
- **MBRF3** — entrada CALL MBRFG166 @0,79 (13:39) — virada, bidirecional (mantém PUT + compra CALL).

### Ordens / dedup (tarefa do cron)
- Pipeline criou 41/42/43 às 18:23; **auto-dedup já resolveu na criação**:
  - LRENG157 (41) → **cancelada** (já tinha executada manual id=39, ticket 5775487360).
  - BEEFG386 (42) → **cancelada** (já tinha executada manual id=37, ticket 5775370441).
  - MBRFG166 (43) → **ticker_invalido** (resolver não achou opção B3 válida).
- **0 ordens em pendente/notificada/fila/aprovada** → nenhuma duplicata pra cancelar manualmente. ✅
- LRENS146 (saída sinalizada): nunca houve ordem nossa → não temos a PUT da LREN; sinal de saída é moot pra carteira.

### O que Luan executou hoje (manual, tarde 16:43–17:11 BRT)
- BEEFG386 CALL exec (t5775370441), BEEFS346 PUT exec (t5775448382) → **strangle no BEEF3**.
- LRENG157 CALL exec (t5775487360).
- MBRFG166 CALL → **erro** (id=40) E ticker_invalido no pipeline → **MBRF NÃO entrou hoje**. ⚠️ ficou de fora.

### Posições reais abertas (5): COGNS273, WEGES439, BEEFG386(CALL), BEEFS346(PUT), LRENG157(CALL).

### ⚠️ Cookie YouTube
Arquivo salvo `youtube-cookies-fresh.txt` está **ROTACIONADO/inválido** (yt-dlp: "no longer valid, rotated"). O fallback launchd roda sem Keychain → depende do cookie salvo → **quebrado** até relogar/reexportar. Próxima vez que o cloud falhar, fallback não salva. Pedir Luan reexportar cookie do Chrome.

## NOITE 2026-06-11 (22h BRT) — recoloc. LREN + diagnóstico MBRF (sessão direta Luan)
- **LREN id=39** (LRENG157 8c @0,75) tinha sido COLOCADA no broker como buy limit mas ficou 0/800 SEM PREENCHER (book ilíquido: bid 0,20/ask 1,00/last 0,76 → limite 0,75 fica abaixo do ask) e expirou ORDER_TIME_DAY (MT5 mostra "canceled" 19:44). id=41 era duplicata (cancelada por mim). Luan ficou sem posição LREN.
- **RECOLOQUEI**: ordem id=46 LRENG157 8c **@0,80** (Luan escolheu), status `pendente`→`notificada` no TG. Quando ele tocar Executar c/ mercado fechado → `fila_abertura` → dispara na abertura (~10h). É demonstração viva do fluxo "ordem com mercado fechado".
- **MERCADO FECHADO = NATIVO na ponte**: order_check volta MARKET_CLOSED(10018) → ponte estaciona `fila_abertura` e re-tenta cada ciclo → dispara na abertura. RESSALVA: é SEMPRE limitada (nunca a mercado, por segurança), então só EXECUTA se limite ≥ ask na abertura.
- **🆕 FATO DURÁVEL — MBRF NÃO TEM OPÇÃO no MT5 da Rico:** varri os 72.492 símbolos. Existe MBRF3 (ação) mas ZERO série de opção sob MBRF/MRFG(Marfrig antiga)/BRFS(BRF antiga). symbol_select("MBRFG166")=False. PORÉM a opção EXISTE na B3 e na plataforma própria da Rico (Luan mandou print: MBRFG166 0,95, vol 210K) e no opcoes.net.br (0,63). ⇒ **gap do FEED do MT5 da Rico** (provável atraso pós-rename Marfrig→MBRF3), não da corretora. Conclusão: **MBRF só dá pra operar MANUAL na Rico até o MT5 carregar a série.** Hipótese do Luan (ticker-mãe antigo) era boa mas refutada: MRFG/BRFS também = 0.
- **Ponte estava DOWN** (log terminava em ^C, sem processo python) → reiniciei via `schtasks /Run /TN NitroBridge`. Reconectou (saldo R$267,95, AUTO_SEND=True). Watchdog devia ter pego (09-23 BRT) — conferir se está vivo.

## NOITE 2026-06-11 (cont.) — YouTube cookieless (PO token) + pre-open cron
- **Pre-open cron criado**: `preopen-bridge-check` (id 90cb2c73), 09:20 BRT seg-sex, sessão agent:trading:main. SSH na VPS → confere/reinicia ponte + confere ordens fila → avisa TG. Garante que a id=46 (e futuras fila_abertura) disparem na abertura.
- **🆕 COOKIE DO YT RESOLVIDO DE VEZ (PO token)**: YouTube exige PO token p/ legenda auto desde 2025; cookie era paliativo que rotaciona diário (insustentável, queixa do Luan). FIX: provider bgutil. Instalei o **plugin yt-dlp** em ~/yt-dlp-plugins/ (elo faltante — o POT server já rodava órfão há 3 dias mas yt-dlp ignorava sem o plugin). POT server agora **launchd com.nitro.potserver** (KeepAlive). `run_local_fallback.sh` reescrito cookieless (ensure_pot+fetch_subs; cadeia HTTP-POT→script-POT→cookie). Validado ponta-a-ponta SEM cookie (legenda 366KB baixou). Detalhe em STATUS.json SOLUCAO_COOKIE_DEFINITIVA.
- **PENDENTE**: push do run_local_fallback.sh pro repo (mudei local). E o cron de captura teve erro de DELIVERY 401 (sendMessage Unauthorized) — é a integração Telegram do GATEWAY com token inválido, separada do bot token que os scripts usam via curl (esses funcionam). Decidir com Luan se ajusta a delivery do cron ou ignora (script já avisa no TG sozinho).
